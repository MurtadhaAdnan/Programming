<div dir="rtl">

# دوال NumPy حسب مستوى الخبرة
# NumPy: المكتبة الأساسية للحوسبة العلمية في بايثون

## `نظرة عامة`
**NumPy** (Numerical Python) هي المكتبة الأساسية للحوسبة الرقمية في بايثون. توفر:

- كائنات **مصفوفات** متعددة الأبعاد عالية الأداء
- أدوات للعمل مع هذه المصفوفات
- دوال رياضية لعمليات المصفوفات <br><br>

## `الفوائد الرئيسية`

| الميزة | الوصف |
|---------|-------------|
| 🚀 **الأداء** | عمليات معتمدة على لغة C (أسرع من القوائم في بايثون) |
| 🔢 **مصفوفات متعددة الأبعاد** | كائن ndarray قوي للمتجهات والمصفوفات والمتانيس |
| 🧮 **المتجهات** | يلغي الحاجة للحلقات عبر البث المصفوفي |
| 🔬 **الأساس العلمي** | قاعدة لمعظم أدوات علم البيانات في بايثون |

___

<br><br><br>

# `المستوى المبتدئ`

| الدالة | الوصف | مثال | النتيجة |
|----------|-------------|---------|--------|
| `np.array()` | إنشاء مصفوفة من بيانات معينة | `np.array([1, 2, 3])` | `array([1, 2, 3])` |
| `np.zeros()` | إنشاء مصفوفة مملوءة بأصفار | `np.zeros((2, 3))` | `array([[0., 0., 0.], [0., 0., 0.]])` |
| `np.ones()` | إنشاء مصفوفة مملوءة بواحدات | `np.ones((2, 2))` | `array([[1., 1.], [1., 1.]])` |
| `np.arange()` | إنشاء مصفوفة بقيم متساوية التباعد | `np.arange(0, 10, 2)` | `array([0, 2, 4, 6, 8])` |
| `np.linspace()` | إنشاء مصفوفة بعدد محدد من القيم متساوية التباعد | `np.linspace(0, 1, 5)` | `array([0., 0.25, 0.5, 0.75, 1.])` |
| `np.shape` | الحصول على أبعاد المصفوفة | `np.array([[1,2],[3,4]]).shape` | `(2, 2)` |
| `np.reshape()` | تغيير شكل المصفوفة دون تغيير البيانات | `np.arange(6).reshape(2, 3)` | `array([[0, 1, 2], [3, 4, 5]])` |
| `np.sum()` | مجموع عناصر المصفوفة | `np.sum([1, 2, 3])` | `6` |
| `np.mean()` | المتوسط الحسابي | `np.mean([1, 2, 3, 4])` | `2.5` |
| `np.max()/np.min()` | القيمة العظمى/الصغرى | `np.max([1, 3, 2])` | `3` |

<br><br><br>

# `المستوى المتوسط`

| الدالة | الوصف | مثال | النتيجة |
|----------|-------------|---------|--------|
| `np.random.rand()` | قيم عشوائية بشكل معين | `np.random.rand(2, 2)` | `array([[0.42, 0.48], [0.18, 0.33]])` (مثال) |
| `np.concatenate()` | ربط مصفوفات على طول محور | `np.concatenate(([1,2], [3,4]))` | `array([1, 2, 3, 4])` |
| `np.vstack()/np.hstack()` | ربط مصفوفات عمودياً/أفقياً | `np.vstack(([1,2], [3,4]))` | `array([[1, 2], [3, 4]])` |
| `np.transpose()` | نقل المصفوفة | `np.transpose([[1,2], [3,4]])` | `array([[1, 3], [2, 4]])` |
| `np.dot()` | الضرب النقطي لمصفوفتين | `np.dot([1,2], [3,4])` | `11` |
| `np.where()` | إرجاع عناصر بناء على شرط | `np.where([True, False, True], 1, 0)` | `array([1, 0, 1])` |
| `np.unique()` | عناصر فريدة في المصفوفة | `np.unique([1, 1, 2, 2, 3])` | `array([1, 2, 3])` |
| `np.sort()` | ترتيب المصفوفة | `np.sort([3, 1, 2])` | `array([1, 2, 3])` |
| `np.argmax()/np.argmin()` | فهارس القيم العظمى/الصغرى | `np.argmax([1, 3, 2])` | `1` |
| `np.loadtxt()/np.savetxt()` | قراءة/كتابة من/إلى ملف نصي | `np.savetxt('data.txt', [1,2,3])` | (حفظ في ملف) |

<br><br><br>

# `المستوى المتقدم`

| الدالة | الوصف | مثال | النتيجة |
|----------|-------------|---------|--------|
| `np.meshgrid()` | إنشاء مصفوفات إحداثيات | `x, y = np.meshgrid([1,2], [3,4])` | مصفوفتان 2x2 |
| `np.einsum()` | اصطلاح جمع أينشتاين | `np.einsum('ii', [[1,2],[3,4]])` | `5` (أثر) |
| `np.polyfit()` | تركيب متعدد حدود بطريقة المربعات الصغرى | `np.polyfit([1,2,3], [1,4,9], 2)` | `array([1., 0., 0.])` (ملاءمة مثالية) |
| `np.fft.fft()` | تحويل فوريي السريع | `np.fft.fft([1, -1, 1, -1])` | مصفوفة مركبة لـ DFT |
| `np.linalg.svd()` | التحليل إلى قيم مفردة | `np.linalg.svd([[1,2],[3,4]])` | مجموعة من U, S, Vh |
| `np.linalg.eig()` | قيم/متجهات ذاتية | `np.linalg.eig([[1, -1], [1, 1]])` | قيم/متجهات ذاتية مركبة |
| `np.pad()` | حشو المصفوفة بقيم | `np.pad([1,2], (1,1), 'constant')` | `array([0, 1, 2, 0])` |
| `np.tile()` | تكرار المصفوفة | `np.tile([1,2], 3)` | `array([1, 2, 1, 2, 1, 2])` |
| `np.vectorize()` | تحويل دالة إلى متجهة | `vfunc = np.vectorize(lambda x: x*2)` | دالة متجهة |
| `np.select()` | إرجاع عناصر بناء على شروط | `np.select([x<1,x>2], [x, x+10])` | اختيار شرطي |

<br><br><br>

# `مستوى الخبير`

| الدالة | الوصف | مثال | النتيجة |
|----------|-------------|---------|--------|
| `np.lib.stride_tricks.as_strided()` | إنشاء عرض مصفوفة بخطوات مخصصة | `as_strided(arr, shape=(3,3), strides=(8,8))` | عرض نافذة منزلقة |
| `np.recarray()` | إنشاء مصفوفة سجلات | `np.recarray((2,), dtype=[('x', int), ('y', float)])` | مصفوفة سجلات |
| `np.memmap()` | تعيين ذاكرة لملف | `np.memmap('data.dat', dtype='float32', mode='r', shape=(3,4))` | مصفوفة معينة بالذاكرة |
| `np.dtype()` | إنشاء نوع بيانات مخصص | `np.dtype([('name', 'S10'), ('age', 'i4')])` | كائن dtype مخصص |
| `np.broadcast_to()` | بث مصفوفة إلى شكل جديد | `np.broadcast_to([1,2,3], (3,3))` | مصفوفة مذاعة |
| `np.apply_along_axis()` | تطبيق دالة على طول محور | `np.apply_along_axis(np.sum, 0, [[1,2],[3,4]])` | `array([4, 6])` |
| `np.partition()` | تقسيم المصفوفة حول عنصر k | `np.partition([3,1,4,2], 2)` | `array([1, 2, 3, 4])` |
| `np.at()` | تنفيذ عملية على فهارس محددة | `np.add.at(arr, [0,1], 10)` | إضافة في المكان |
| `np.putmask()` | تعيين قيم بناء على قناع | `np.putmask(arr, arr>5, 0)` | مصفوفة معدلة |
| `np.isin()` | اختبار وجود عناصر في مصفوفة أخرى | `np.isin([1,2,3], [2,4])` | `array([False, True, False])` |

<br><br><br>

# `مستوى الخبير المتمرس`

| الدالة | الوصف | مثال | النتيجة |
|----------|-------------|---------|--------|
| `np.nditer()` | مكرر متعدد الأبعاد فعال | `for x in np.nditer(arr): print(x)` | يتكرر عبر العناصر |
| `np.ufunc.reduceat()` | اختزال في فهارس محددة | `np.add.reduceat([1,2,3,4], [0,2])` | `array([3, 7])` |
| `np.interp()` | استكمال خطي أحادي البعد | `np.interp(2.5, [1,2,3], [10,20,30])` | `25.0` |
| `np.gradient()` | تدرج المصفوفة | `np.gradient([1,2,4,7])` | `array([1., 1.5, 2.5, 3.])` |
| `np.correlate()` | ارتباط متبادل | `np.correlate([1,2,3], [0,1,0.5])` | `3.5` |
| `np.histogramdd()` | هيستوجرام متعدد الأبعاد | `np.histogramdd(sample, bins=3)` | هيستوجرام وحواف |
| `np.polynomial` | حزمة متعددات الحدود | `np.polynomial.Polynomial.fit(x, y, 2)` | كائن متعدد الحدود |
| `np.lib.mixins.NDArrayOperatorsMixin` | إنشاء فئات متوافقة مع NumPy | `class MyArray(NDArrayOperatorsMixin): ...` | فئة مصفوفة مخصصة |
| `np.einsum_path()` | تحسين مسار einsum | `np.einsum_path('ij,jk,kl->il', a, b, c)` | مسار التحسين |
| `np.can_cast()` | التحقق من إمكانية التحويل بين الأنواع | `np.can_cast(np.int32, np.float64)` | `True` |

</div>