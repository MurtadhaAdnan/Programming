

# دوال NumPy حسب مستوى الخبرة
# NumPy: المكتبة الأساسية للحوسبة العلمية في بايثون

## `نظرة عامة`
**NumPy** (Numerical Python) هي المكتبة الأساسية للحوسبة الرقمية في بايثون. توفر:

- كائنات **مصفوفات** متعددة الأبعاد عالية الأداء
- أدوات للعمل مع هذه المصفوفات
- دوال رياضية لعمليات المصفوفات <br><br>

## `الفوائد الرئيسية`

| الميزة | الوصف |
|---------|-------------|
| 🚀 **الأداء** | عمليات معتمدة على لغة C (أسرع من القوائم في بايثون) |
| 🔢 **مصفوفات متعددة الأبعاد** | كائن ndarray قوي للمتجهات والمصفوفات والمتانيس |
| 🧮 **المتجهات** | يلغي الحاجة للحلقات عبر البث المصفوفي |
| 🔬 **الأساس العلمي** | قاعدة لمعظم أدوات علم البيانات في بايثون |

___

<br><br><br>

## `مستوى مبتدئ`

| الدالة | الوصف | مثال | النتيجة |
|--------|-------|------|---------|
| `np.array()` | إنشاء مصفوفة من بيانات معينة | `np.array([1, 2, 3])` | `array([1, 2, 3])` |
| `np.zeros()` | إنشاء مصفوفة مملوءة بأصفار | `np.zeros((2, 3))` | `array([[0., 0., 0.], [0., 0., 0.]])` |
| `np.ones()` | إنشاء مصفوفة مملوءة بواحدات | `np.ones((2, 2))` | `array([[1., 1.], [1., 1.]])` |
| `np.arange()` | إنشاء مصفوفة بقيم متساوية التباعد | `np.arange(0, 10, 2)` | `array([0, 2, 4, 6, 8])` |
| `np.linspace()` | إنشاء مصفوفة بعدد محدد من القيم متساوية التباعد | `np.linspace(0, 1, 5)` | `array([0., 0.25, 0.5, 0.75, 1.])` |
| `np.shape` | الحصول على أبعاد المصفوفة | `np.array([[1,2],[3,4]]).shape` | `(2, 2)` |
| `np.reshape()` | تغيير شكل المصفوفة دون تغيير البيانات | `np.arange(6).reshape(2, 3)` | `array([[0, 1, 2], [3, 4, 5]])` |
| `np.sum()` | حساب مجموع عناصر المصفوفة | `np.sum([1, 2, 3])` | `6` |
| `np.mean()` | حساب المتوسط الحسابي | `np.mean([1, 2, 3, 4])` | `2.5` |
| `np.max()` | إيجاد القيمة العظمى | `np.max([1, 3, 2])` | `3` |
| `np.min()` | إيجاد القيمة الصغرى | `np.min([1, 3, 2])` | `1` |

## `مستوى متوسط`

| الدالة | الوصف | مثال | النتيجة |
|--------|-------|------|---------|
| `np.random.rand()` | إنشاء مصفوفة بأعداد عشوائية | `np.random.rand(2, 2)` | `array([[0.42, 0.48], [0.18, 0.33]])` |
| `np.concatenate()` | دمج مصفوفات على طول محور معين | `np.concatenate(([1,2], [3,4]))` | `array([1, 2, 3, 4])` |
| `np.vstack()` | دمج مصفوفات عمودياً | `np.vstack(([1,2], [3,4]))` | `array([[1, 2], [3, 4]])` |
| `np.hstack()` | دمج مصفوفات أفقياً | `np.hstack(([1,2], [3,4]))` | `array([1, 2, 3, 4])` |
| `np.transpose()` | نقل المصفوفة (تبديل الصفوف بالأعمدة) | `np.transpose([[1,2], [3,4]])` | `array([[1, 3], [2, 4]])` |
| `np.dot()` | ضرب المصفوفات | `np.dot([1,2], [3,4])` | `11` |
| `np.where()` | اختيار عناصر بناء على شرط | `np.where([True, False, True], 1, 0)` | `array([1, 0, 1])` |
| `np.unique()` | إيجاد العناصر الفريدة في المصفوفة | `np.unique([1, 1, 2, 2, 3])` | `array([1, 2, 3])` |
| `np.sort()` | ترتيب عناصر المصفوفة | `np.sort([3, 1, 2])` | `array([1, 2, 3])` |
| `np.argmax()` | إيجاد مؤشر القيمة العظمى | `np.argmax([1, 3, 2])` | `1` |
| `np.argmin()` | إيجاد مؤشر القيمة الصغرى | `np.argmin([1, 3, 2])` | `0` |
| `np.loadtxt()` | قراءة بيانات من ملف نصي | `np.loadtxt('data.txt')` | بيانات الملف |
| `np.savetxt()` | حفظ بيانات إلى ملف نصي | `np.savetxt('data.txt', [1,2,3])` | حفظ في ملف |

## `مستوى متقدم`

| الدالة | الوصف | مثال | النتيجة |
|--------|-------|------|---------|
| `np.meshgrid()` | إنشاء مصفوفات إحداثيات من متجهات | `x, y = np.meshgrid([1,2], [3,4])` | مصفوفتان إحداثيتان |
| `np.einsum()` | تنفيذ عمليات جبرية معقدة | `np.einsum('ii', [[1,2],[3,4]])` | `5` |
| `np.polyfit()` | تركيب متعدد حدود لبيانات | `np.polyfit([1,2,3], [1,4,9], 2)` | `array([1., 0., 0.])` |
| `np.fft.fft()` | تحويل فوريي السريع | `np.fft.fft([1, -1, 1, -1])` | تحويل فوريي |
| `np.linalg.svd()` | التحليل إلى قيم مفردة | `np.linalg.svd([[1,2],[3,4]])` | (U, S, Vh) |
| `np.linalg.eig()` | حساب القيم والمتجهات الذاتية | `np.linalg.eig([[1, -1], [1, 1]])` | قيم ومتجهات ذاتية |
| `np.pad()` | حشو المصفوفة بقيم معينة | `np.pad([1,2], (1,1), 'constant')` | `array([0, 1, 2, 0])` |
| `np.tile()` | تكرار المصفوفة | `np.tile([1,2], 3)` | `array([1, 2, 1, 2, 1, 2])` |
| `np.vectorize()` | تحويل دالة عادية لدالة تعمل على المصفوفات | `vfunc = np.vectorize(lambda x: x*2)` | دالة متجهة |
| `np.select()` | اختيار عناصر بناء على شروط متعددة | `np.select([x<1,x>2], [x, x+10])` | اختيار شرطي |

## `مستوى خبير`

| الدالة | الوصف | مثال | النتيجة |
|--------|-------|------|---------|
| `np.lib.stride_tricks.as_strided()` | إنشاء views للمصفوفات بخطوات معينة | `as_strided(arr, shape=(3,3), strides=(8,8))` | عرض نافذة منزلقة |
| `np.recarray()` | إنشاء مصفوفات مع حقول مسماة | `np.recarray((2,), dtype=[('x', int), ('y', float)])` | مصفوفة مسجلة |
| `np.memmap()` | العمل مع مصفوفات كبيرة في الذاكرة الثانوية | `np.memmap('data.dat', dtype='float32')` | مصفوفة معينة بالذاكرة |
| `np.dtype()` | تعريف أنواع بيانات مخصصة | `np.dtype([('name', 'S10'), ('age', 'i4')])` | نوع بيانات مخصص |
| `np.broadcast_to()` | بث المصفوفة إلى شكل معين | `np.broadcast_to([1,2,3], (3,3))` | مصفوفة مذاعة |
| `np.apply_along_axis()` | تطبيق دالة على طول محور معين | `np.apply_along_axis(np.sum, 0, [[1,2],[3,4]])` | `array([4, 6])` |
| `np.partition()` | تقسيم المصفوفة حول محور معين | `np.partition([3,1,4,2], 2)` | `array([1, 2, 3, 4])` |
| `np.at()` | تنفيذ عمليات بإضافة على عناصر محددة | `np.add.at(arr, [0,1], 10)` | إضافة في المكان |
| `np.putmask()` | وضع قيم بناء على قناع معين | `np.putmask(arr, arr>5, 0)` | مصفوفة معدلة |
| `np.isin()` | اختبار وجود عناصر في مصفوفة أخرى | `np.isin([1,2,3], [2,4])` | `array([False, True, False])` |

## `مستوى محترف`

| الدالة | الوصف | مثال | النتيجة |
|--------|-------|------|---------|
| `np.nditer()` | التكرار على المصفوفات بكفاءة عالية | `for x in np.nditer(arr): print(x)` | تكرار العناصر |
| `np.ufunc.reduceat()` | تنفيذ عمليات مختزلة على شرائح محددة | `np.add.reduceat([1,2,3,4], [0,2])` | `array([3, 7])` |
| `np.interp()` | استكمال قيم بين نقاط معينة | `np.interp(2.5, [1,2,3], [10,20,30])` | `25.0` |
| `np.gradient()` | حساب تدرج المصفوفة | `np.gradient([1,2,4,7])` | `array([1., 1.5, 2.5, 3.])` |
| `np.correlate()` | حساب الارتباط بين متجهين | `np.correlate([1,2,3], [0,1,0.5])` | `3.5` |
| `np.histogramdd()` | حساب متعدد الأبعاد للهيستوجرام | `np.histogramdd(sample, bins=3)` | هيستوجرام متعدد الأبعاد |
| `np.polynomial` | التعامل مع متعددات الحدود المتقدمة | `np.polynomial.Polynomial.fit(x, y, 2)` | كائن متعدد الحدود |
| `np.lib.mixins.NDArrayOperatorsMixin` | إنشاء فئات متوافقة مع NumPy | `class MyArray(NDArrayOperatorsMixin): ...` | فئة مصفوفة مخصصة |
| `np.einsum_path()` | تحسين مسار حساب einsum | `np.einsum_path('ij,jk,kl->il', a, b, c)` | مسار محسن |
| `np.can_cast()` | التحقق من إمكانية التحويل بين أنواع البيانات | `np.can_cast(np.int32, np.float64)` | `True` |
